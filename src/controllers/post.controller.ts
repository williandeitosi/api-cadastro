import type { Request, Response } from 'express'

export const getAllPosts = async (req: Request, res: Response) => {
  // TODO: change name function for get post user, list user with post, not list all posts
}
